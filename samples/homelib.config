# Some variables you may use in this configuration file (these are filled out
# in the `Main.py' script):
#
# %(dokumentidir)s
# %(homedir)s           - The currently configured home directory.
# %(nastavitvedir)s
# %(nestdir)s           - The currently configured directory of the 'Nest' repository.
# %(osebnodir)s
# %(projectsdir)s
# %(researchdir)s
# %(homelibconfdir)s    - The directory which contains per-user private homelib configuration data.



[General Information]

name:Matej
surname:Urbas
email:matej@urbas.si

topDomain:urbas.si

# This value indicates which 'Nest Service' implementation should be used.
nestService:hg
# Indicates the section within this configuration file that should be used by
# the Nest Service Provider.
nestServiceSection:hg.cfg

# This value indicates which 'Software Service' implementation should be used.
softwareService:yum

# Some other important folders:
TEMPLATES_DIR=%(nastavitvedir)s/Templates

# The directory where my Bash notes and scripts are stored.
CONFIGURATION_DIR=%(nastavitvedir)s/Configuration

# The directory where my Bash notes and scripts are stored.
MY_BASH_DIR=%(nastavitvedir)s/Bash

# The folder where all my local scripts are (these will go into the '~/bin'
# folder).
MY_BASH_LOCAL_BIN_DIR=%(MY_BASH_DIR)s/bin

# The folder where all my 'bash' includes are.
MY_BASH_INCLUDE_DIR=%(MY_BASH_DIR)s/include

# The folder where all my 'nautilus scripts' includes are.
MY_NAUTILUS_SCRIPTS_DIR=%(MY_BASH_DIR)s/Nautilus Scripts

# Here are all my 'template' related things:
TEMPLATES_DIR=%(nastavitvedir)s/Templates

# Meeting memos and action points folder:
MEETING_MEMO_DIR=%(researchdir)s/Meeting Memos and Stuff

# The directory where my LaTeX notes and scripts are stored.
MY_LATEX_DIR=%(nastavitvedir)s/LaTeX
MY_LATEX_CONTRIB_DIR=%(MY_LATEX_DIR)s/contrib

# This directory contains Fedora-specific files.
MY_FEDORA_DIR=%(nastavitvedir)s/Fedora
MY_FEDORA_CONFIG_DIR=%(MY_FEDORA_DIR)s/Config Files

# Here are all my 'video' related things:
VIDEO_DIR=%(nastavitvedir)s/Video
VIDEO_FILES_DIR=%(VIDEO_DIR)s/Files

# This directory contains Maco-specific configuration files.
MACO_DIR=%(nastavitvedir)s/Maco

# The folder where to store all logs.
logDir=%(homelibconfdir)s/logs

# This directory contains Git configuration files.
MY_GIT_CONFIG_DIR=%(nastavitvedir)s/Git/Configuration



[hg.cfg]

repoPath:/maco/hg/Nest
port:8444
domain:urbas.si
repoUri:%(domain)s:%(port)s/%(repoPath)s

defProto:ssh
defUser:matej
defFullRepoUri:%(defProto)s://%(defUser)s@%(repoUri)s
defCloneName:Nest



# One of the configuration sections for the 'Configuration' service. This one
# takes care of 'per-user' configuration.
[Configuration]
# This is the python script that contains a class that implements the
# 'homelib.config.ConfigScript' class. It performs the actual configuration of
# the machine/home folder.
0.cfgScriptName=homeconfig
0.cfgScriptClass=HomeConfig
0.cfgScriptPath=%(nastavitvedir)s/Configuration/HomeConfig/src
# This is the directory where the persistent configuration progress information
# should be stored.
0.cfgScriptStore=%(homedir)s/.si.urbas.homeconfig
0.cfgScriptDescription=Configures my home folders and keeps them updated.

1.cfgScriptName=machineconfig
1.cfgScriptClass=MachineConfig
1.cfgScriptPath=%(nastavitvedir)s/Configuration/MachineConfig/src
1.cfgScriptStore=/var/lib/mymachineconfig
1.cfgScriptDescription=Configures my machines (both personal computers and the Maco server) and keeps them updated. Root access is required to run this script.



[My Machines]

0.hostname=maco.urbas.si
0.description=My main server. Stationed at home (Maribor, Slovenia). Currently this is the fanless Shuttle computer with the Atom CPU.
0.type=maco_server

1.hostname=terra.urbas.si
1.description=My very first laptop. An HP Compaq nc8430 bought on December 2006.
1.type=laptop

2.hostname=toncka.urbas.si
2.description=My second laptop and my first tablet. An HP EliteBook 2730p tablet bought on December 2008.
2.type=laptop

3.hostname=mersey.cl.cam.ac.uk
3.description=My office computer in Cambridge.
3.type=cl_office

4.hostname=tali.urbas.si
4.description=My third laptop. A Dell XPS 17 gaming notebook bought December 2010.
4.type=laptop

5.hostname=maco1.urbas.si
5.description=This is the previous Maco server. The HP desktop computer.
5.type=laptop,home_desktop
